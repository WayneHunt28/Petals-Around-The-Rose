[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "potentate"
version = "0.1.0"
dependencies = [
    "rich
]

[tool.setuptools.packages.find]
where = ["src"]